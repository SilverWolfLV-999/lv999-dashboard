# =================================================================
# 认证配置（Clerk）
# =================================================================
# 快速开始：运行 `npx clerk@latest init`，几秒钟即可完成 Clerk 配置 —— 无需
# 注册账号。它会自动创建开发环境实例，并将密钥写入 .env.local。
# 更想使用自己的密钥？请到 https://dashboard.clerk.com 获取并粘贴到下方。

# 必填：Clerk API 密钥

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# 认证重定向 URL
# 这些配置控制用户在完成认证操作后的跳转目标。
# *_FALLBACK_REDIRECT_URL 仅在请求中没有 `redirect_url` 时生效（例如直接
# 访问受保护页面的深层链接时，若未登录则回退到此地址）；一般来说用户会
# 返回到最初请求的页面。如果希望无论用户来自哪里都固定跳转到某个页面，
# 请改用 *_FORCE_REDIRECT_URL。

NEXT_PUBLIC_CLERK_SIGN_IN_URL="/auth/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/auth/sign-up"
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/dashboard/overview"
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL="/dashboard/overview"

# =================================================================
# Admin Backend (User Management)
# =================================================================
# 平台管理员白名单：逗号分隔的 Clerk userId（如 user_xxx,user_yyy）。
# 命中白名单的账号才能在账号下拉看到「用户管理」入口、访问
# /dashboard/admin/users 并调用 /api/admin/* 端点（列用户 / 调 Credits / 删用户）。
# 你自己的 userId 可在 Clerk Dashboard > Users 查看，或登录后从 auth().userId 取。
# 安全默认：留空 = 无管理员，管理页对所有人不可达。

ADMIN_USER_IDS=    # 示例："user_2abcDEF123456"

# =================================================================
# Clerk Organizations (REMOVED — 本项目不再使用多租户)
# =================================================================
# 组织 / 工作区 / 团队功能已从本项目移除（核心业务全部按 userId 隔离）。
# 如需重新启用，可在 Clerk Dashboard > Organizations 开启并配置默认角色，
# 参见 https://clerk.com/docs/organizations/overview （对本项目为可选，无需配置）。

# =================================================================
# Clerk Webhooks（可选）
# =================================================================
# 如果你使用 Clerk Webhooks，请在此填写 webhook 密钥。
# 可在 Clerk Dashboard > Webhooks 中找到该密钥

WEBHOOK_SECRET=    # 示例：whsec_...

# =================================================================
# 应用配置
# =================================================================
# 应用的公开基础 URL，用于 metadataBase（Open Graph / SEO）。

NEXT_PUBLIC_APP_URL=    # 示例："https://your-domain.com"

# =================================================================
# 构建配置
# =================================================================
# 使用 Docker 部署或在 VPS 上自托管时设为 'true'。
# 这会启用 Next.js standalone 输出模式，以便更精简地部署。

BUILD_STANDALONE=    # 示例："true"

# =================================================================
# 重要提示：
# =================================================================
# 1. 本地开发时请将此文件重命名为 '.env'
# 2. 切勿将实际的 '.env' 文件提交到版本控制系统
# 3. 确保将所有占位值替换为真实值
# 4. 妥善保管密钥，切勿外泄

# =================================================================
# Agent 创作模块（阿里云 RDS PostgreSQL / 阿里云百炼 / 阿里云 OSS）
# =================================================================

# 阿里云 RDS PostgreSQL 连接串（华东1 杭州；Serverless 实例不支持 SSL，驱动会自动回退明文连接）
# 示例："postgresql://lv999:<password>@pgm-xxxxxxx.rwlb.rds.aliyuncs.com:5432/lv999-dashboard"
# 注意：密码中的特殊字符需要 URL 编码（如 @ -> %40、# -> %23）
DATABASE_URL=

# 阿里云百炼（Model Studio）API Key — https://bailian.console.aliyun.com
DASHSCOPE_API_KEY=

# 阿里云 OSS（Phase 2 起用于存储图片/视频等二进制产物）
# 手动步骤：创建杭州 region 私有 bucket，并为部署域名配置 CORS
OSS_REGION=oss-cn-hangzhou
OSS_BUCKET=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=

# Redis（流恢复 resumable-stream 与停止信号使用，需 pub/sub 支持的 TLS 连接串）
# 推荐 Upstash（Vercel Marketplace 或 upstash.com），区域建议 Tokyo
REDIS_URL=    # 示例："rediss://default:password@region.upstash.io:6379"
